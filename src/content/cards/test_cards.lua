
g.defineCard("basic_slash", {
    image = "basic_slash",
    cost = 1,
    onCastPlayer = function(card, player)
        print("todo")
    end,
    onCast = function(card, player)
        print("todo")
    end,
})

g.defineCard("basic_shield", {
    image = "basic_shield",
    cost = 1,
    onCastPlayer = function(card, player)
        print("todo")
    end,
    onCast = function(card, player)
        print("todo")
    end,
})

g.defineCard("radial_slash", {
    image = "radial_slash",
    cost = 2,
    onCastPlayer = function(card, player)
        print("todo")
    end,
    onCast = function(card, player)
        print("todo")
    end,
})

g.defineCard("heroic_dash", {
    image = "heroic_dash",
    cost = 1,
    onCastPlayer = function(card, player)
        print("todo")
    end,
    onCast = function(card, player)
        print("todo")
    end,
})

g.defineCard("wing_dash", {
    image = "wing_dash",
    cost = 1,
    onCastPlayer = function(card, player)
        print("todo")
    end,
    onCast = function(card, player)
        print("todo")
    end,
})


